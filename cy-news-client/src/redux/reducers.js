import {AUTH_SUCCESS,ERROR_MSG} from './action-types'

var initUser = {
    u_userid: '',
    u_username: '',
    header: ''
}

function user(state=initUser,action) {
    switch (action.type) {
        case AUTH_SUCCESS:
            cosnt {} = 
            break;
        case ERROR_MSG:
            
            break;
    
        default:
            break;
    }
}